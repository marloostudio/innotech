/**
 * Default address for team copies of Contact and Demo form submissions.
 * Override per flow with `LEADS_NOTIFY_EMAIL` or `DEMO_REQUEST_NOTIFY_EMAIL`.
 */
export const DEFAULT_INTERNAL_INBOX = "info@innotech-sys.com" as const
